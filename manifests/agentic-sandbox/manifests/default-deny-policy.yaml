{{$applyDefaultDenyPolicy := .ApplyDefaultDenyPolicy}}

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{.Name}}
  namespace: {{.Namespace}}
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
